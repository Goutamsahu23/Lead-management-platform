const LEAD_STATUSES = ['new', 'contacted', 'qualified', 'won', 'lost'];
const USER_ROLES = ['admin', 'member'];
const ACTIVITY_TYPES = ['created', 'status_changed', 'assigned', 'note_added'];

module.exports = {
  LEAD_STATUSES,
  USER_ROLES,
  ACTIVITY_TYPES,
};
